note: if we run as REPL, we should see 'done' in stdout
between: { 'repl >>>' } { '<<<' }
  regexp: ^^ done $$
end:

note: if we run as script, we should see 'done' in stdout
between: { 'as_script >>>' } { '<<<' }
  regexp: ^^ done $$
end:
